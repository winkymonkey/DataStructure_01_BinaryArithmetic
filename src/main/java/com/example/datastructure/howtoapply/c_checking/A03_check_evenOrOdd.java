package com.example.datastructure.howtoapply.c_checking;

/**
 * *****************************************************************************
 * Check a number is odd or even without modulus operator
 * *****************************************************************************
 */	
public class A03_check_evenOrOdd {
	/*
	 * Need to check whether the LSB is 0 or 1.
	 * if LSB is 1 --- odd
	 * if LSB is 0 --- even
	 * 
	 * n = 100;
	 * if((n & 1) == 1)
	 * 	 //print "ODD"
	 * else
	 * 	 //print "EVEN"
	 * 
	 */
}
